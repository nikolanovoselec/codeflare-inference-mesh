# Cloudflare Inference Mesh Plan

Status: planning document

This document defines the architecture and implementation plan for Cloudflare
Inference Mesh. It is intentionally explanatory. It describes what the system
is, why the components exist, how they relate, which Cloudflare APIs and
features are involved, what alternatives were considered, and how to build the
system in phases.

## Table Of Contents

1. [Purpose](#purpose)
2. [Product Goal](#product-goal)
3. [Design Principles](#design-principles)
4. [Architecture Summary](#architecture-summary)
5. [Component Responsibilities](#component-responsibilities)
6. [Core Cloudflare References](#core-cloudflare-references)
7. [End-To-End Flows](#end-to-end-flows)
8. [Security And Trust Model](#security-and-trust-model)
9. [Router Worker Architecture](#router-worker-architecture)
10. [Durable Object Registry](#durable-object-registry)
11. [Node Agent Architecture](#node-agent-architecture)
12. [Model Runtime Strategy](#model-runtime-strategy)
13. [Admin Setup Flow](#admin-setup-flow)
14. [AI Gateway Setup Flow](#ai-gateway-setup-flow)
15. [Model Virtualization And Stable Gateway Names](#model-virtualization-and-stable-gateway-names)
16. [GitHub Actions And Releases](#github-actions-and-releases)
17. [Cloudflare API Token Scopes](#cloudflare-api-token-scopes)
18. [Observability](#observability)
19. [Failure Modes](#failure-modes)
20. [Alternatives Considered](#alternatives-considered)
21. [Implementation Phases](#implementation-phases)
22. [Validation Gates](#validation-gates)
23. [Open Decisions](#open-decisions)

## Purpose

The purpose of this project is to expose several private local LLM machines as
one OpenAI-compatible model provider behind Cloudflare AI Gateway.

The intended user experience is simple:

```text
Coding agent or app
  -> Cloudflare AI Gateway
  -> one custom provider
  -> one dynamic route or model name
  -> best available private local machine
  -> local llama.cpp or compatible inference server
```

The user should not need to know which machine is serving a request. A coding
agent should call one AI Gateway URL, while the router decides whether the
request should go to the office RTX 3090, a Mac Studio, a Linux workstation, or
another enrolled machine.

## Product Goal

Build a private inference mesh with these properties:

- One AI Gateway custom provider endpoint for all local inference nodes.
- Multiple Windows, macOS, and Linux nodes.
- Nodes reachable privately through Cloudflare One Client, WARP, Mesh, and
  Workers VPC.
- A Cloudflare Worker router that receives OpenAI-compatible requests.
- A Durable Object registry that tracks node health, capacity, model support,
  session affinity, and in-flight reservations.
- A local node agent with a browser UI for setup and observability.
- One-line node installation generated by the router admin UI.
- GitHub Actions for Worker deploys and node-agent releases.

The first useful version should route AI Gateway traffic to one WARP-enrolled
machine running an existing local llama.cpp server. Multi-node scheduling and
fully managed model downloads come after that path is proven.

## Design Principles

### Keep The Public Surface Small

Only the Worker is public. Local nodes do not expose public hostnames and do not
need inbound port forwarding. The Worker enforces application-level bearer
tokens for AI Gateway, node registration, and node upstream calls.

### Separate Control Plane From Data Plane

Inference traffic is the data plane:

```text
AI Gateway -> Worker -> selected node -> local runtime
```

Registration, setup, health, model profiles, and admin screens are the control
plane:

```text
Admin UI / node agent -> Worker -> Durable Object
```

Separating these concepts avoids leaking admin credentials into inference
calls and keeps routing logic understandable.

### Prefer Private Network Routing Over Public Per-Node URLs

The preferred transport is Workers VPC to Cloudflare Mesh. Each node registers
its private Mesh IP and port. The Worker builds the target URL itself. Nodes do
not register arbitrary URLs.

This reduces public exposure and avoids managing one Cloudflare Tunnel hostname
per machine.

### Preserve Session Affinity

Long-context coding work is expensive to prefill. If a session jumps between
machines, each node has to rebuild its cache. The router should prefer sending
all requests for a coding session to the same node, even if another node is
currently idle.

### Make The Node Agent Opinionated

The node agent should not ask users for Cloudflare account IDs, Durable Object
names, model flags, or AI Gateway details. Those belong to the router and
model profiles. A node should need only:

```text
router URL
setup token
optional node name
```

### Start Narrow, Then Generalize

Do not build the full desktop product before proving the Cloudflare transport.
The first milestone is:

```text
AI Gateway -> Worker -> Workers VPC Mesh fetch -> one WARP client -> llama.cpp
```

## Architecture Summary

### Target Architecture

```text
Client / coding agent
  -> Cloudflare AI Gateway
  -> AI Gateway custom provider
  -> Cloudflare Worker router
  -> Durable Object registry and scheduler
  -> Workers VPC binding
  -> Cloudflare Mesh
  -> WARP-enrolled node
  -> node agent
  -> local llama.cpp / Ollama / LM Studio / vLLM
```

### Logical Planes

```text
Provider plane
  AI Gateway custom provider and dynamic route.

Router plane
  Worker HTTP API, auth checks, request forwarding, streaming.

State plane
  Durable Object registry, scheduling, node leases, session affinity.

Node plane
  Cross-platform agent, local UI, runtime supervision, model management.

Release plane
  GitHub Actions, Worker deploys, agent builds, installer artifacts.
```

### Data Plane Request Path

```text
1. Client calls AI Gateway dynamic route.
2. AI Gateway calls custom provider URL on the Worker.
3. Worker verifies the provider bearer token.
4. Worker parses the OpenAI-compatible request.
5. Worker maps the public model name to the active internal model profile.
6. Worker asks Durable Object for a node reservation.
7. Durable Object selects an eligible node.
8. Worker sends the request through Workers VPC to the node Mesh IP.
9. Node agent validates the upstream token.
10. Node agent forwards to local inference runtime.
11. Runtime response streams back through the same path.
12. Worker releases the reservation when the stream ends or fails.
```

### Control Plane Node Path

```text
1. Admin creates a short-lived setup token in Worker setup UI.
2. Admin copies a one-line install command.
3. Installer starts the node agent.
4. Node agent claims setup token.
5. Worker returns permanent node credentials and model profile.
6. Node agent detects Mesh IP and starts heartbeat.
7. Durable Object stores the live node lease.
```

## Component Responsibilities

### Cloudflare AI Gateway

Purpose:

- Public AI gateway used by clients and coding agents.
- Logging, dynamic routing, provider abstraction, and optional BYOK/provider key
  storage.

Responsibilities:

- Receive client requests.
- Route selected model or dynamic route to the inference mesh custom provider.
- Send the provider bearer token when calling the Worker, if configured through
  BYOK/provider key storage.
- Apply configured timeout and retry settings.

AI Gateway should not:

- Know about individual local machines.
- Hold node registration tokens.
- Dispatch directly to WARP client IPs.
- Know which concrete local model is currently active behind a stable router
  alias.

References:

- AI Gateway overview:
  <https://developers.cloudflare.com/ai-gateway/>
- Custom providers:
  <https://developers.cloudflare.com/ai-gateway/configuration/custom-providers/>
- Dynamic routing:
  <https://developers.cloudflare.com/ai-gateway/features/dynamic-routing/>
- Bring Your Own Keys:
  <https://developers.cloudflare.com/ai-gateway/configuration/bring-your-own-keys/>

### Cloudflare Worker Router

Purpose:

- Act as the OpenAI-compatible custom provider endpoint.
- Hide local node topology from AI Gateway.
- Enforce authentication and routing policy.

Responsibilities:

- Serve `/v1/models` and `/v1/chat/completions`.
- Verify `ROUTER_PROVIDER_TOKEN` on provider API requests.
- Forward chat completions to selected nodes through Workers VPC.
- Stream node responses back to AI Gateway.
- Serve first-run setup UI on the `workers.dev` URL.
- Optionally provision a custom domain after first-run configuration.
- Create or update the AI Gateway custom provider and dynamic route.
- Serve install scripts.
- Accept node claim and heartbeat requests.

The Worker should stay mostly stateless. Routing state belongs in the Durable
Object.

References:

- Workers:
  <https://developers.cloudflare.com/workers/>
- Workers runtime APIs:
  <https://developers.cloudflare.com/workers/runtime-apis/>
- Workers Streams:
  <https://developers.cloudflare.com/workers/runtime-apis/streams/>
- Workers custom domains:
  <https://developers.cloudflare.com/workers/configuration/routing/custom-domains/>
- Workers secrets:
  <https://developers.cloudflare.com/workers/configuration/secrets/>

### Durable Object Registry

Purpose:

- Provide one strongly coordinated place for live node state and scheduling.

Responsibilities:

- Store node records and leases.
- Track in-flight reservations.
- Track sessions and sticky node mappings.
- Apply model alias mapping.
- Score eligible nodes.
- Return 429 when capacity is exhausted.
- Expire stale nodes.

Why Durable Objects:

- The scheduler needs consistent in-flight counts.
- Two Worker isolates must not reserve the same single-GPU node at the same
  time.
- Durable Objects provide per-object serialized execution and persistent
  storage, which fits registry and scheduling state.

References:

- Durable Objects:
  <https://developers.cloudflare.com/durable-objects/>
- Durable Object storage:
  <https://developers.cloudflare.com/durable-objects/api/storage-api/>
- Durable Object alarms:
  <https://developers.cloudflare.com/durable-objects/api/alarms/>

### Workers VPC And Cloudflare Mesh

Purpose:

- Let the Worker call private WARP-enrolled node IPs without public node
  hostnames.

Expected Worker binding:

```jsonc
{
  "vpc_networks": [
    {
      "binding": "MESH",
      "network_id": "cf1:network",
      "remote": true
    }
  ]
}
```

Expected Worker call:

```ts
const response = await env.MESH.fetch(
  "http://100.96.12.34:11434/v1/chat/completions",
  requestInit
);
```

Important operating assumptions:

- The node must be enrolled in Cloudflare Zero Trust through Cloudflare One
  Client, not only consumer WARP.
- Mesh or device IPs must be enabled.
- The local node listener must bind to the Mesh IP or another address reachable
  from the WARP interface.
- The local firewall must permit inbound traffic on the node API port from the
  Mesh path.
- The Worker builds URLs from validated `meshIp` and `port`. The node never
  registers a full URL.

References:

- Workers VPC:
  <https://developers.cloudflare.com/workers-vpc/>
- Workers VPC to Cloudflare Mesh example:
  <https://developers.cloudflare.com/workers-vpc/examples/connect-to-cloudflare-mesh/>
- Cloudflare Mesh:
  <https://developers.cloudflare.com/cloudflare-one/networks/connectors/cloudflare-mesh/>
- Cloudflare One Client downloads:
  <https://developers.cloudflare.com/cloudflare-one/team-and-resources/devices/cloudflare-one-client/download/>

### Node Agent

Purpose:

- Make each local machine self-contained and observable.
- Shield the router from OS-specific details.
- Manage local runtime lifecycle in a consistent way.

Responsibilities:

- Run as a background service on Windows, macOS, and Linux.
- Serve a local UI on `http://127.0.0.1:17777`.
- Detect WARP status and Mesh IP.
- Claim setup tokens.
- Store permanent node credentials.
- Start and stop the inference engine.
- Download and verify model files in later phases.
- Enforce local bearer-token auth for Worker-to-node calls.
- Report metrics and heartbeat to the Worker.

The node agent should not require the user to configure Cloudflare API details.
It receives its router URL, credentials, and model profile from the setup flow.

## Core Cloudflare References

This plan depends on these documented Cloudflare features:

| Area | Why it matters | Documentation |
| --- | --- | --- |
| AI Gateway Custom Providers | Lets AI Gateway call the Worker as a model provider | <https://developers.cloudflare.com/ai-gateway/configuration/custom-providers/> |
| AI Gateway BYOK | Stores provider credentials so clients do not need the router token | <https://developers.cloudflare.com/ai-gateway/configuration/bring-your-own-keys/> |
| AI Gateway Dynamic Routing | Lets a named route point to the custom provider and model | <https://developers.cloudflare.com/ai-gateway/features/dynamic-routing/> |
| AI Gateway API | Needed for setup UI automation of providers and routes | <https://developers.cloudflare.com/api/resources/ai_gateway/> |
| Workers VPC | Lets Workers fetch private network endpoints | <https://developers.cloudflare.com/workers-vpc/> |
| Workers VPC Mesh Example | Shows `cf1:network` style Worker-to-Mesh connectivity | <https://developers.cloudflare.com/workers-vpc/examples/connect-to-cloudflare-mesh/> |
| Cloudflare Mesh | Private WARP device networking | <https://developers.cloudflare.com/cloudflare-one/networks/connectors/cloudflare-mesh/> |
| Cloudflare One Client | Required on node machines for WARP enrollment | <https://developers.cloudflare.com/cloudflare-one/team-and-resources/devices/cloudflare-one-client/download/> |
| Durable Objects | Registry and scheduler state | <https://developers.cloudflare.com/durable-objects/> |
| Workers Streams | Streaming OpenAI SSE responses through the Worker | <https://developers.cloudflare.com/workers/runtime-apis/streams/> |
| Workers Custom Domains | Optional custom domain provisioned from setup UI | <https://developers.cloudflare.com/workers/configuration/routing/custom-domains/> |
| Workers Secrets | Runtime tokens and provider tokens | <https://developers.cloudflare.com/workers/configuration/secrets/> |
| API Token Permissions | Required Cloudflare token scopes | <https://developers.cloudflare.com/fundamentals/api/reference/permissions/> |
| Wrangler GitHub Action | Deploy Worker from GitHub Actions | <https://github.com/cloudflare/wrangler-action> |
| GitHub Actions Syntax | CI and release workflow structure | <https://docs.github.com/en/actions/writing-workflows/workflow-syntax-for-github-actions> |
| GoReleaser GitHub Actions | Cross-platform agent builds and checksums | <https://goreleaser.com/ci/actions/> |
| llama.cpp Server | Default local OpenAI-compatible runtime | <https://github.com/ggml-org/llama.cpp/tree/master/tools/server> |
| Qwen3.6 27B model card | Qwen long-context coding profile candidate | <https://huggingface.co/Qwen/Qwen3.6-27B> |
| Gemma 4 model overview | Alternative local long-context model family | <https://ai.google.dev/gemma/docs/core> |
| Gemma 4 model card | Gemma 4 context length, modalities, model details | <https://ai.google.dev/gemma/docs/core/model_card_4> |
| Gemma 4 GGUF docs | Local Gemma 4 GGUF profiles and memory guidance | <https://unsloth.ai/docs/models/gemma-4> |

## End-To-End Flows

### First Deployment Flow

Intent:

The first deployment should require only GitHub repository secrets and should
bring up a Worker on its `workers.dev` hostname. The Worker can then configure
itself through a first-run admin screen.

Flow:

```text
1. User adds GitHub secrets.
2. GitHub Actions deploys Worker to workers.dev.
3. User opens the Worker setup URL.
4. User authenticates with first-run setup token.
5. User optionally configures custom domain.
6. User selects Cloudflare AI Gateway.
7. User enters route name, for example freestyler.
8. Worker creates custom provider pointing to itself.
9. Worker creates dynamic route using that provider.
10. Worker creates provider bearer token and displays it once.
11. User adds that token as the AI Gateway provider key or BYOK value.
12. Worker generates node install command.
```

Why this way:

- It avoids requiring Cloudflare Access on day one.
- It avoids manual dashboard-only setup for custom providers and dynamic routes.
- It keeps the Worker usable before a custom domain exists.
- It gives a clean split between bootstrap token and long-lived provider token.

### Node Install Flow

Linux and macOS command:

```bash
curl -fsSL https://inference-mesh-router.example.workers.dev/install.sh | sudo IM_SETUP_TOKEN=abc123 sh
```

Windows PowerShell command:

```powershell
$env:IM_SETUP_TOKEN="abc123"; irm https://inference-mesh-router.example.workers.dev/install.ps1 | iex
```

Flow:

```text
1. Installer downloads the correct node-agent artifact.
2. Installer verifies checksum.
3. Installer installs the service.
4. Installer writes minimal bootstrap config.
5. Installer starts the local agent UI.
6. Agent calls POST /node/claim with setup token.
7. Worker returns permanent node token, upstream token, and model profile.
8. Agent detects Mesh IP.
9. Agent starts heartbeat.
10. Agent appears online in router admin UI.
```

Why this way:

- Install scripts carry no permanent secret.
- Setup tokens are short-lived and single-use.
- Local machines do not need Cloudflare API tokens.
- The node can be rebuilt or rotated without touching AI Gateway config.

### Inference Flow

Flow:

```text
1. Client sends OpenAI-compatible request to AI Gateway.
2. AI Gateway resolves dynamic route to custom provider.
3. AI Gateway calls Worker `/v1/chat/completions`.
4. Worker validates provider token.
5. Worker maps requested public model name to active internal profile.
6. Worker asks Durable Object for a reservation.
7. Durable Object selects a node.
8. Worker calls node agent through `env.MESH.fetch`.
9. Node agent validates upstream token.
10. Node agent forwards to local runtime.
11. Response streams back to AI Gateway.
12. Worker releases reservation.
```

Why this way:

- AI Gateway remains the only endpoint clients need.
- Durable Object prevents overbooking a single-GPU node.
- Worker-to-node token prevents arbitrary callers on the Mesh from using the
  runtime.
- Streaming preserves OpenAI-compatible behavior.

## Security And Trust Model

### Trust Boundaries

The system has four different trust boundaries. They must use separate
credentials.

```text
Client -> AI Gateway
  AI Gateway auth or client auth, depending on the user's Gateway policy.

AI Gateway -> Worker
  ROUTER_PROVIDER_TOKEN.

Node agent -> Worker
  Per-node token issued after setup-token claim.

Worker -> node agent
  NODE_UPSTREAM_TOKEN or per-node upstream token.
```

### Token Classes

```text
ROUTER_PROVIDER_TOKEN
  Stored in AI Gateway as provider key or BYOK value.
  Used only by AI Gateway when calling Worker `/v1/*`.

INIT_SETUP_TOKEN
  Initial bootstrap token used to open first-run setup.
  Stored as GitHub secret or generated once by deploy workflow.

ONE_TIME_SETUP_TOKEN
  Short-lived token generated by admin UI for a node install.
  Used once by `/node/claim`.

NODE_TOKEN
  Permanent per-node bearer token.
  Used by node heartbeats and unregister calls.

NODE_UPSTREAM_TOKEN
  Token Worker sends when calling node local API.
  Can be global for MVP and per-node later.

CLOUDFLARE_API_TOKEN_RUNTIME
  Stored only as Worker secret.
  Used by setup UI to provision Cloudflare resources.
```

### Why Not One Token

One shared token would be easier, but it creates bad blast radius:

- A compromised node could impersonate AI Gateway.
- A leaked provider token could register fake nodes.
- A copied install command could enroll unlimited machines.
- A local runtime token leak could grant admin access.

Separate tokens let each breach be contained and rotated independently.

### Public Routes And Auth

Recommended route behavior:

```text
GET /
GET /setup
  First-run setup UI.
  Requires INIT_SETUP_TOKEN until setup is complete.

GET /admin/*
POST /admin/*
  Requires admin session or ADMIN_TOKEN.
  Cloudflare Access is optional later, not required for MVP.

GET /install.sh
GET /install.ps1
  Public read is acceptable.
  Scripts contain no permanent secret.

POST /node/claim
  Public route.
  Requires ONE_TIME_SETUP_TOKEN.

POST /node/heartbeat
POST /node/unregister
  Public route.
  Requires NODE_TOKEN.

GET /v1/models
POST /v1/chat/completions
  Public route from Cloudflare perspective.
  Requires ROUTER_PROVIDER_TOKEN.
```

### Cloudflare Access Position

Cloudflare Access is useful for human admin UI later. It should not be required
for the first version because:

- AI Gateway would need to send Access service-token headers.
- Node agents would also need Access service-token headers.
- Access does not replace app-level node identity.

First version:

```text
workers.dev setup UI
  protected by first-run token and later admin token/session

custom domain
  optional, configured from setup UI

Access
  optional hardening step after core path works
```

## Router Worker Architecture

### Repository Location

Planned package:

```text
packages/router-worker
```

### Worker Inputs

Runtime secrets:

```text
ROUTER_PROVIDER_TOKEN
ADMIN_TOKEN
NODE_UPSTREAM_TOKEN
CLOUDFLARE_API_TOKEN_RUNTIME
```

Runtime vars:

```text
CLOUDFLARE_ACCOUNT_ID
NODE_LEASE_SECONDS
DEFAULT_NODE_PORT
ALLOWED_MESH_CIDRS
```

Wrangler bindings:

```jsonc
{
  "durable_objects": {
    "bindings": [
      {
        "name": "REGISTRY",
        "class_name": "RegistryDO"
      }
    ]
  },
  "vpc_networks": [
    {
      "binding": "MESH",
      "network_id": "cf1:network",
      "remote": true
    }
  ]
}
```

### Public API

Provider endpoints:

```text
GET  /v1/models
POST /v1/chat/completions
GET  /health
```

Node endpoints:

```text
POST /node/claim
POST /node/heartbeat
POST /node/unregister
```

Admin and setup endpoints:

```text
GET  /
GET  /setup
GET  /admin
GET  /admin/status
GET  /admin/nodes
GET  /admin/routes
POST /admin/setup-tokens
POST /admin/nodes/:nodeId/revoke
POST /admin/cloudflare/custom-domain
POST /admin/cloudflare/ai-gateway
```

Installer endpoints:

```text
GET /install.sh
GET /install.ps1
```

### Request Handling Rules

The Worker should route requests in this order:

```text
1. Health checks.
2. Static setup/admin UI assets.
3. Installer scripts.
4. Node claim and heartbeat endpoints.
5. Admin endpoints.
6. Provider auth for `/v1/*`.
7. OpenAI-compatible provider endpoints.
8. 404.
```

Do not put provider auth in front of `/node/*`, `/install.*`, or setup UI.
Those routes use different auth.

### Chat Completion Handling

Required behavior:

```text
1. Read JSON body.
2. Validate request body size.
3. Normalize `model`.
4. Extract session ID.
5. Reserve node in Durable Object.
6. Rewrite model to node upstream model name.
7. Forward to node through `env.MESH.fetch`.
8. Stream upstream body.
9. Release reservation on stream end, error, or missing body.
10. Add observability headers.
```

Required headers to node:

```http
Authorization: Bearer <NODE_UPSTREAM_TOKEN>
Content-Type: application/json
X-Inference-Mesh-Request-Id: <request_id>
X-Inference-Mesh-Session: <session_id>
```

Do not forward these headers to nodes:

```text
client Authorization
Cloudflare API token
admin token
node registration token
AI Gateway metadata that contains secrets
```

### Streaming Rules

For `stream: true`, pass through Server-Sent Events unchanged:

```text
node SSE stream -> Worker body -> AI Gateway -> client
```

For long context, prefill can take many seconds before the first token. The
plan should prefer:

- `stream: true` for coding agents.
- AI Gateway timeout of at least 60 seconds, higher if supported.
- AI Gateway retries set to 0 or 1 for local long-running routes.
- Worker retries only before generation starts.
- No keepalive bytes for non-stream JSON responses.

SSE comment keepalives can be tested later, but only if AI Gateway and the
client tolerate them.

## Durable Object Registry

### Node Record

```ts
type NodeRecord = {
  nodeId: string;
  displayName: string;
  meshIp: string;
  port: number;
  engine: "llama.cpp" | "ollama" | "lmstudio" | "vllm";
  status: "starting" | "ready" | "busy" | "draining" | "error";
  models: ModelRecord[];
  capacity: {
    maxConcurrent: number;
    weight?: number;
    vramGb?: number;
  };
  metrics: NodeMetrics;
  inFlight: number;
  lastSeenAt: number;
  expiresAt: number;
  failurePenaltyUntil?: number;
  recentFailures: number;
};
```

### Model Record

```ts
type ModelRecord = {
  name: string;
  publicAliases: string[];
  upstreamModel: string;
  context: number;
  loaded: boolean;
  quant?: string;
  profileId?: string;
  profileVersion?: number;
};
```

### Session Record

```ts
type SessionRecord = {
  sessionId: string;
  nodeId: string;
  model: string;
  createdAt: number;
  lastUsedAt: number;
  expiresAt: number;
};
```

### Reservation Record

```ts
type ReservationRecord = {
  reservationId: string;
  nodeId: string;
  model: string;
  sessionId?: string;
  createdAt: number;
  expiresAt: number;
};
```

### Node Eligibility

A node is eligible if:

```text
now < expiresAt
status == ready
requested model or alias is supported
model is loaded
inFlight < maxConcurrent
not under failure penalty
meshIp is valid
port is allowed
```

### Scheduling Score

Recommended first scoring function:

```text
score =
  configured weight
  + session affinity bonus
  - inFlight penalty
  - gpu utilization penalty
  - gpu temperature penalty
  - recent failure penalty
```

For coding workloads, the session affinity bonus should dominate.

### Session ID Sources

Preferred:

```http
X-Inference-Mesh-Session: codex-pi-main
```

Fallback:

```http
cf-aig-metadata: {"session":"codex-pi-main"}
```

Weak fallback:

```text
hash(provider token fingerprint + model + user-agent + first system message)
```

The weak fallback is useful only when the client cannot set headers. It should
not be treated as security identity.

### Busy Behavior

If no node is available:

```http
HTTP/1.1 429 Too Many Requests
Retry-After: 5
Content-Type: application/json
```

Response body:

```json
{
  "error": {
    "message": "No available inference node for model mesh-default",
    "type": "inference_mesh_busy"
  }
}
```

Do not return 500 for normal capacity exhaustion.

## Node Agent Architecture

### Repository Location

Planned package:

```text
packages/node-agent
```

### Language And UI Choice

Use Go for the agent.

Why:

- One binary per OS and architecture.
- Practical Windows service support.
- Practical Linux systemd support.
- Practical macOS launchd support.
- Strong HTTP, process, filesystem, and JSON support.
- Straightforward cross-platform release builds.

Use a local web UI embedded in the Go binary:

```text
http://127.0.0.1:17777
```

Why not a native desktop framework first:

- The agent is primarily a service.
- Headless Linux nodes need the same binary.
- A browser UI avoids Electron, Tauri, Wails, or platform GUI installers in the
  first version.
- The UI can still be polished and interactive.

### Agent Runtime Responsibilities

The agent should:

- Run as a background service.
- Serve local UI and API on `127.0.0.1`.
- Detect Cloudflare One Client and Mesh IP.
- Claim setup token.
- Store node token and upstream token.
- Heartbeat to Worker.
- Listen for Worker-to-node inference requests on Mesh-reachable address.
- Enforce `Authorization: Bearer <NODE_UPSTREAM_TOKEN>`.
- Proxy OpenAI-compatible requests to local runtime.
- Start and stop runtime process.
- Report runtime state and metrics.
- Drain before shutdown.

### Agent Local API

Local UI API:

```text
GET  /api/status
GET  /api/logs/recent
GET  /api/config
POST /api/setup/claim
POST /api/model/download
POST /api/runtime/start
POST /api/runtime/stop
POST /api/runtime/restart
```

Mesh-facing API:

```text
GET  /health
GET  /v1/models
POST /v1/chat/completions
```

The local UI should bind to `127.0.0.1`. The Mesh-facing API can bind to the
Mesh IP if possible. If binding to Mesh IP is unreliable across OSes, fallback
to `0.0.0.0` plus bearer-token auth and firewall rules.

### Agent Dashboard Fields

Node:

```text
node ID
display name
OS and architecture
agent version
service uptime
```

Router:

```text
router URL
registration status
last heartbeat
heartbeat latency
last heartbeat error
```

Network:

```text
WARP status
Mesh IP
listening address
listening port
firewall status if detectable
```

Model:

```text
active model
model profile
model file path
download status
loaded or unloaded
context length
quantization
```

Runtime:

```text
engine type
engine process PID
engine state
current request state
in-flight requests
prompt tokens per second
output tokens per second
current prompt tokens
last request duration
```

Hardware:

```text
GPU name
GPU utilization
GPU temperature
VRAM used and free
CPU utilization
system memory used and free
```

### Service Installation

Windows:

```text
InferenceMeshAgent.exe install
InferenceMeshAgent.exe start
```

Linux:

```text
inference-mesh-agent install
systemctl enable --now inference-mesh-agent
```

macOS:

```text
inference-mesh-agent install
launchctl bootstrap system /Library/LaunchDaemons/com.novoselec.inference-mesh.plist
```

## Model Runtime Strategy

### Default Engine: llama.cpp

Use `llama-server` as the default engine.

Why:

- Cross-platform.
- Supports CUDA, Metal, Vulkan, ROCm, and CPU builds depending on platform.
- Exposes OpenAI-compatible endpoints.
- Gives direct control over long-context flags.
- Allows tuning context length, flash attention, KV cache quantization, GPU
  layers, aliases, and reasoning format when supported.

Reference:

- llama.cpp server:
  <https://github.com/ggml-org/llama.cpp/tree/master/tools/server>

### Model Profiles

The router owns model profiles. The agent consumes them. A model profile is the
internal runtime definition for one concrete model on one hardware class.

External clients and AI Gateway should not depend on these concrete names.
They should call stable public aliases such as:

```text
mesh-default
freestyler
code-worker
```

The router maps those aliases to active profiles.

### Qwen 3.6 27B Profile

```json
{
  "id": "qwen36-27b-256k-3090",
  "label": "Qwen 3.6 27B, 256K context, RTX 3090",
  "engine": "llama.cpp",
  "publicAliases": ["mesh-default", "freestyler"],
  "model": {
    "name": "qwen3.6:27b",
    "repo": "unsloth/Qwen3.6-27B-GGUF",
    "file": "Q4_K_M.gguf",
    "sha256": "optional-pinned-checksum"
  },
  "runtime": {
    "context": 262144,
    "maxConcurrent": 1,
    "args": [
      "-ngl", "99",
      "-c", "262144",
      "-fa", "on",
      "--cache-type-k", "q4_0",
      "--cache-type-v", "q4_0",
      "--no-mmproj"
    ]
  }
}
```

This is the first serious coding-agent profile to test on an RTX 3090. It is
the strongest current Qwen open-weight local candidate for long-context coding
work.

### Gemma 4 26B-A4B Profile

```json
{
  "id": "gemma4-26b-a4b-256k-3090",
  "label": "Gemma 4 26B-A4B, 256K context, RTX 3090",
  "engine": "llama.cpp",
  "publicAliases": ["mesh-default", "freestyler"],
  "model": {
    "name": "gemma4:26b-a4b",
    "repo": "unsloth/gemma-4-26B-A4B-it-GGUF",
    "file": "UD-Q4_K_M.gguf",
    "sha256": "optional-pinned-checksum"
  },
  "runtime": {
    "context": 262144,
    "maxConcurrent": 1,
    "args": [
      "-ngl", "99",
      "-c", "262144",
      "-fa", "on",
      "--cache-type-k", "q4_0",
      "--cache-type-v", "q4_0",
      "--no-mmproj"
    ]
  }
}
```

Why Gemma 4 26B-A4B is in scope:

- Google documents Gemma 4 as open weights with long-context support up to
  256K tokens.
- The 26B-A4B variant is a consumer-GPU-sized MoE candidate.
- It is a useful competitor to Qwen3.6-27B for coding-agent quality, latency,
  and reasoning behavior.
- It can be served through `llama-server` as an OpenAI-compatible endpoint when
  using a compatible GGUF build.

Gemma caveats:

- Use a recent llama.cpp build because Gemma 4 compatibility has changed over
  time.
- Test text-only first. Multimodal support is not required for the coding-agent
  route and may need additional projector/runtime handling.
- Treat 256K as a validation target, not a guarantee. Runtime overhead and KV
  cache behavior decide whether the 3090 profile is stable.

Why profiles live in the router:

- New installs get updated defaults without rebuilding the agent.
- Different hardware classes can get different profiles.
- Admin can control which models are allowed.
- AI Gateway route names can map cleanly to model aliases.
- Public AI Gateway names can stay stable while the active internal model
  changes from Qwen to Gemma or back.

### Initial Runtime Scope

Phase 1 does not need automatic model download. It can use an existing local
llama.cpp server. This proves networking and AI Gateway behavior first.

Phase 4 adds:

- llama.cpp binary management.
- model download.
- checksum verification.
- process supervisor.
- one-click Start and Stop.

## Admin Setup Flow

### Initial Bootstrap

The Worker first deploys on `workers.dev`.

Expected bootstrap:

```text
1. GitHub Actions deploys Worker.
2. Deploy workflow creates or uses INIT_SETUP_TOKEN.
3. User opens workers.dev setup URL.
4. User enters INIT_SETUP_TOKEN.
5. Worker stores setup-complete state in Durable Object.
6. Worker generates provider token.
7. Worker shows provider token once.
```

The setup page should not require Cloudflare Access in MVP.

### Optional Custom Domain

The setup UI can ask for:

```text
zone
hostname
```

Example:

```text
inference-mesh-router.novoselec.ch
```

The Worker uses `CLOUDFLARE_API_TOKEN_RUNTIME` to create or attach the Worker
custom domain.

Why custom domain is optional:

- `workers.dev` is enough for first deployment and testing.
- Custom domain can fail because of zone permission or DNS conflicts.
- AI Gateway custom providers only need an HTTPS base URL.

Reference:

- Workers custom domains:
  <https://developers.cloudflare.com/workers/configuration/routing/custom-domains/>

## AI Gateway Setup Flow

### Custom Provider

The Worker setup UI should create or update an AI Gateway custom provider that
points at the Worker public URL.

Example provider:

```json
{
  "name": "Inference Mesh",
  "slug": "inference-mesh",
  "base_url": "https://inference-mesh-router.novoselec.ch",
  "description": "Worker router for WARP/Mesh local inference nodes",
  "enable": true
}
```

Expected provider endpoint:

```text
https://gateway.ai.cloudflare.com/v1/<account_id>/<gateway_id>/custom-inference-mesh/v1/chat/completions
```

Reference:

- AI Gateway custom providers:
  <https://developers.cloudflare.com/ai-gateway/configuration/custom-providers/>

### Provider Bearer Token

The Worker expects:

```http
Authorization: Bearer <ROUTER_PROVIDER_TOKEN>
```

The setup UI should show the token once and instruct the user to add it as the
provider key or BYOK value for the custom provider.

Do not store the token in plaintext if avoidable. Store either:

- a hash for verification, if the Worker only needs to verify incoming tokens
- an encrypted value, if the Worker needs to display or rotate it later

Reference:

- BYOK:
  <https://developers.cloudflare.com/ai-gateway/configuration/bring-your-own-keys/>

### Dynamic Route

The setup UI should let the user select an existing AI Gateway and enter a
route name.

Example:

```text
gateway: codeflare-enterprise
route name: freestyler
provider: custom-inference-mesh
model: mesh-default
timeout: 60000 or higher
retries: 0 or 1
```

Why retries should be low:

- Local long-context generations are expensive.
- AI Gateway retries can start duplicate generations.
- Duplicate generations can saturate one-GPU nodes.

References:

- Dynamic routing:
  <https://developers.cloudflare.com/ai-gateway/features/dynamic-routing/>
- AI Gateway API:
  <https://developers.cloudflare.com/api/resources/ai_gateway/>

### Required Validation

The first implementation must verify exactly what headers AI Gateway sends to
the Worker after provider key configuration.

Temporary debug endpoint:

```text
POST /debug/headers
```

Rules:

- Log header names only.
- Never log token values.
- Delete or disable after validation.

## Model Virtualization And Stable Gateway Names

### Intent

AI Gateway should be configured once and then left alone. The router should own
model choice, model rollout, and node-agent profile propagation.

The external contract should be stable:

```text
AI Gateway route: freestyler
AI Gateway provider: custom-inference-mesh
AI Gateway provider model: mesh-default
Worker public model: mesh-default
```

The internal implementation can change:

```text
mesh-default -> qwen36-27b-256k-3090
mesh-default -> gemma4-26b-a4b-256k-3090
mesh-default -> small-smoke-test-32k
```

This lets the administrator switch between Qwen, Gemma, smaller smoke-test
models, or hardware-specific profiles without reconfiguring AI Gateway.

### Public Model Alias

The Worker should expose public models from router config:

```json
{
  "publicModels": [
    {
      "name": "mesh-default",
      "description": "Default local inference mesh model",
      "activeProfileId": "gemma4-26b-a4b-256k-3090",
      "fallbackProfileIds": [
        "qwen36-27b-256k-3090"
      ]
    }
  ]
}
```

`GET /v1/models` should return public aliases, not every internal runtime name.

Example response:

```json
{
  "object": "list",
  "data": [
    {
      "id": "mesh-default",
      "object": "model",
      "owned_by": "inference-mesh"
    }
  ]
}
```

### Request Rewriting

Inbound request from AI Gateway:

```json
{
  "model": "mesh-default",
  "messages": [],
  "stream": true
}
```

Router lookup:

```text
mesh-default
  -> activeProfileId: gemma4-26b-a4b-256k-3090
  -> internal model name: gemma4:26b-a4b
```

Outbound request to selected node:

```json
{
  "model": "gemma4:26b-a4b",
  "messages": [],
  "stream": true
}
```

The AI Gateway route stays configured with `mesh-default`. Only the Worker
router config changes.

### Profile Versioning

Model profiles need a version so node agents can detect changes:

```json
{
  "profileId": "gemma4-26b-a4b-256k-3090",
  "profileVersion": 3,
  "model": "gemma4:26b-a4b",
  "context": 262144
}
```

The Durable Object should store:

```text
public model alias
active profile ID
active profile version
compatible node IDs
rollout state
```

Node heartbeat should include:

```json
{
  "nodeId": "win3090-office",
  "readyProfiles": [
    {
      "profileId": "qwen36-27b-256k-3090",
      "profileVersion": 1,
      "loaded": true
    }
  ],
  "activeRuntimeProfile": "qwen36-27b-256k-3090"
}
```

Heartbeat response can include desired state:

```json
{
  "desiredProfiles": [
    {
      "profileId": "gemma4-26b-a4b-256k-3090",
      "profileVersion": 3,
      "action": "prepare"
    }
  ],
  "activePublicModels": [
    {
      "publicModel": "mesh-default",
      "activeProfileId": "gemma4-26b-a4b-256k-3090"
    }
  ]
}
```

### Rollout Flow

Safe model swap:

```text
1. Admin changes `mesh-default` desired profile to Gemma 4.
2. Router increments profile version.
3. Node agents receive desired profile on heartbeat.
4. Nodes download or prepare Gemma 4.
5. Nodes start runtime or warm model.
6. Nodes heartbeat `readyProfiles` including Gemma 4.
7. Router switches `mesh-default` active profile when enough nodes are ready.
8. New requests route to Gemma 4.
9. Old Qwen profile remains available for rollback.
```

For MVP, the rollout can be manual:

```text
admin changes active profile
node prepares profile
admin switches active alias when node is ready
```

Later, the router can support canary and automatic readiness gates.

### Compatibility With AI Gateway

This architecture is compatible with AI Gateway because AI Gateway only sees a
normal OpenAI-compatible provider:

```text
POST /v1/chat/completions
model: mesh-default
```

The Worker performs all internal model selection and rewrites the model name
before forwarding to the node. AI Gateway does not need to know whether the
node is running Qwen, Gemma, or another local runtime.

### Compatibility With OpenAI APIs

The first supported endpoint should be:

```text
POST /v1/chat/completions
```

The Worker can add these later:

```text
POST /v1/responses
POST /v1/completions
POST /v1/embeddings
```

Do not make `/v1/completions` the primary target for Gemma 4. Gemma 4 is an
instruction/chat model, and the router should normalize coding agents onto
chat completions first.

### Why This Is Better Than Reconfiguring AI Gateway

Stable public aliases give:

- one permanent AI Gateway custom provider
- one permanent dynamic route
- one permanent client configuration
- router-controlled model swaps
- fast rollback
- node-agent propagation through heartbeat responses

The cost is that the Worker must own alias mapping, profile versioning, and
request rewriting. That is the right place for this logic because the Worker
already owns scheduling and node selection.

## GitHub Actions And Releases

### Planned Repository Layout

```text
cloudflare-inference-mesh/
  packages/
    router-worker/
      src/
      wrangler.jsonc
      package.json

    node-agent/
      cmd/inference-mesh-agent/
      internal/
      web/
      go.mod

  docs/
    setup.md
    operations.md

  PLAN.md
  README.md

  .github/
    workflows/
      ci.yml
      release-node-agent.yml
      deploy-worker.yml
```

### CI Workflow

`ci.yml` should run on pull requests and pushes.

Router checks:

```text
npm ci
npm test
npm run typecheck
wrangler deploy --dry-run
```

Agent checks:

```text
go test ./...
go vet ./...
build embedded UI
go build
```

### Worker Deploy Workflow

`deploy-worker.yml` should run on pushes to `main`.

Responsibilities:

```text
checkout
setup node
npm ci
npm test
wrangler deploy
set Worker secrets if changed through protected workflow
```

Reference:

- Wrangler GitHub Action:
  <https://github.com/cloudflare/wrangler-action>

### Node Agent Release Workflow

`release-node-agent.yml` should run on version tags:

```yaml
on:
  push:
    tags:
      - "v*"
```

Release artifacts:

```text
inference-mesh-agent_windows_amd64.zip
inference-mesh-agent_windows_arm64.zip
inference-mesh-agent_darwin_amd64.tar.gz
inference-mesh-agent_darwin_arm64.tar.gz
inference-mesh-agent_linux_amd64.tar.gz
inference-mesh-agent_linux_arm64.tar.gz
checksums.txt
```

Use GoReleaser or a similar release workflow to build, package, checksum, and
publish artifacts.

Reference:

- GoReleaser GitHub Actions:
  <https://goreleaser.com/ci/actions/>

## Cloudflare API Token Scopes

Use scoped Cloudflare API tokens, not the global API key.

Reference:

- Cloudflare API token permissions:
  <https://developers.cloudflare.com/fundamentals/api/reference/permissions/>

### GitHub Repository Secrets

Required:

```text
CLOUDFLARE_ACCOUNT_ID
  Cloudflare account ID.

CLOUDFLARE_API_TOKEN_DEPLOY
  Used by GitHub Actions to deploy the Worker.

CLOUDFLARE_API_TOKEN_RUNTIME
  Stored as Worker secret.
  Used by the Worker setup UI to provision Cloudflare resources.
```

Optional:

```text
INFERENCE_MESH_INIT_TOKEN
  First-run setup token.
  If omitted, deploy workflow can generate one and print it once in the Actions
  summary.

CLOUDFLARE_ZONE_ID
  Optional default zone for custom domain setup.
```

### Deploy Token

Used only by GitHub Actions.

Minimum account permissions:

```text
Workers Scripts Edit
```

Optional account permissions:

```text
Workers Tail Read
  Only if CI collects live logs.

Secrets Store Edit
  Only if deploy workflow writes account-level Secrets Store entries.
```

Do not give DNS permissions to the deploy token unless deploy workflow itself
creates custom domains.

### Runtime Token

Stored as a Worker secret.

Used by setup UI to:

- list zones for custom domain setup
- attach a custom domain to the Worker
- list AI Gateways
- create or update AI Gateway custom provider
- create or update dynamic route

Minimum account permissions:

```text
AI Gateway Read
AI Gateway Edit
Workers Scripts Read
Workers Scripts Edit
```

Minimum zone permissions for selected zone:

```text
Zone Read
DNS Read
DNS Write
Workers Routes Read
Workers Routes Edit
```

If the Worker later automates BYOK secret creation instead of instructing the
user to add the provider key manually, add:

```text
Secrets Store Edit
```

Until BYOK automation exists, do not grant `Secrets Store Edit`.

## Observability

### Response Headers

Add these headers to provider responses:

```http
X-Inference-Mesh-Node: win3090-office
X-Inference-Mesh-Request-Id: req_...
X-Inference-Mesh-Session: codex-pi-main
```

### Router Status

`GET /admin/status` should return:

```json
{
  "nodes": [
    {
      "nodeId": "win3090-office",
      "status": "ready",
      "publicModels": ["mesh-default"],
      "activeProfiles": ["gemma4-26b-a4b-256k-3090"],
      "inFlight": 1,
      "maxConcurrent": 1,
      "lastSeenSecondsAgo": 8,
      "metrics": {
        "gpuUtil": 97,
        "gpuTemp": 73,
        "tokensPerSecond": 22.4
      }
    }
  ],
  "sessions": [
    {
      "sessionId": "codex-pi-main",
      "nodeId": "win3090-office",
      "publicModel": "mesh-default",
      "profileId": "gemma4-26b-a4b-256k-3090",
      "upstreamModel": "gemma4:26b-a4b"
    }
  ]
}
```

### Node Metrics

Heartbeat should report:

```text
runtime state
model loaded
in-flight requests
last request duration
prompt tokens per second
output tokens per second
GPU utilization
GPU temperature
VRAM free and used
WARP status
Mesh IP
```

## Failure Modes

### Node Stops Heartbeating

Behavior:

- Lease expires.
- Node is removed from eligible routing.
- Existing session mapping can remain briefly.
- If node does not return, next request can reroute or return 429.

### Node Is Busy

Behavior:

- If session affinity is not required, try another compatible node.
- If the request belongs to a hot sticky session, prefer 429 over moving it.

Reason:

Moving a long-context coding session can force another huge prefill and make
latency worse.

### Node Crashes Mid-Stream

Behavior:

- Release reservation.
- Increment recent failure count.
- Apply failure penalty.
- Return upstream stream failure or 502.
- Do not automatically retry after generation has started.

### WARP Disconnects

Behavior:

- Node heartbeat fails or reports disconnected.
- Lease expires.
- Router stops selecting node.

### AI Gateway Retries

Risk:

AI Gateway can retry a request and create duplicate local generations.

Mitigation:

- Set retries to 0 or 1 for long local routes.
- Let Worker retry only before generation starts.

### Context Cache Thrash

Risk:

Requests for one coding session bounce between nodes.

Mitigation:

- Explicit session ID.
- Sticky session mapping in Durable Object.
- Prefer busy response over moving a hot session.

## Alternatives Considered

### Per-Node Cloudflare Tunnel Hostnames

Alternative:

```text
Worker -> https://node-a.example.com
Worker -> https://node-b.example.com
```

Pros:

- Mature Cloudflare Tunnel path.
- Easier to test with existing Nginx Proxy Manager or cloudflared setup.
- No dependency on Workers VPC to Mesh behavior.

Cons:

- One public hostname per node.
- More DNS and Tunnel configuration.
- More Access/service-token complexity.
- Larger public attack surface.

Decision:

Use as fallback only. Prefer Workers VPC and Mesh for primary design.

### AI Gateway Directly To Each Node

Alternative:

```text
AI Gateway dynamic route -> per-node custom providers
```

Pros:

- Less custom router code.

Cons:

- AI Gateway does not know local node health or Mesh IPs.
- Hard to enforce session affinity.
- Hard to reserve single-GPU capacity.
- Node credentials and URLs leak into provider config.

Decision:

Reject. Keep one Worker router.

### Concrete Model Names In AI Gateway

Alternative:

```text
AI Gateway route model: qwen3.6:27b
AI Gateway route model: gemma4:26b-a4b
```

Pros:

- Direct and easy to understand.
- Less alias mapping logic in the Worker.

Cons:

- Every model change requires AI Gateway route edits.
- Client configuration can drift from router state.
- Rollback requires Cloudflare route changes instead of one router config
  change.
- Node-agent desired state cannot be coordinated cleanly with Gateway config.

Decision:

Reject for the main product. Use stable public aliases such as `mesh-default`
or `freestyler` in AI Gateway, then let the Worker map those aliases to active
internal profiles.

### KV, D1, Or R2 Instead Of Durable Object

Pros:

- Simple persistent storage.
- Easy inspection.

Cons:

- Scheduler needs serialized reservation logic.
- KV eventual consistency is wrong for in-flight counts.
- D1 still needs transaction design and is not naturally colocated with router
  arbitration.
- R2 is object storage, not scheduler state.

Decision:

Use Durable Object for registry and scheduling.

### Cloudflare Access As Primary Auth

Pros:

- Strong human login story.
- Good admin protection.

Cons:

- AI Gateway and node agents need Access service-token headers.
- Access does not replace node identity.
- Adds friction to the first working version.

Decision:

Use app-level bearer tokens first. Add Access to admin UI later if desired.

### Native Desktop App

Options:

```text
Tauri
Wails
Electron
```

Pros:

- More native desktop feel.

Cons:

- Extra packaging complexity.
- Poor fit for headless Linux.
- Not necessary for a service dashboard.

Decision:

Use Go service with embedded local web UI.

### Ollama Or LM Studio As Default Engine

Pros:

- Easier manual setup.
- Familiar UI in LM Studio.
- Ollama has convenient CLI model management.

Cons:

- Less control over long-context flags.
- Harder to reproduce exact performance settings.
- More awkward for one-button managed service behavior.

Decision:

Use llama.cpp as default. Add adapters later.

### vLLM As Default Engine

Pros:

- Strong throughput on Linux NVIDIA.
- Production-grade OpenAI-compatible server.

Cons:

- Not cleanly cross-platform.
- GPU and CUDA expectations make Windows and macOS harder.

Decision:

Keep vLLM as optional Linux backend, not default.

### Public GitHub Releases Versus Worker Download Broker

Public release artifacts:

- Simple installers.
- Easy checksum verification.
- Source repo can remain private only if releases/artifacts are public enough.

Worker download broker:

- Keeps artifacts private.
- Requires Worker to serve binaries from R2 or GitHub with server-side token.

Decision:

Start with GitHub Release artifacts and checksums if acceptable. Use R2 or
Worker broker if private binary distribution is required.

## Implementation Phases

### Phase 1: Prove Worker To One WARP Client

Goal:

```text
AI Gateway -> Worker -> one WARP client Mesh IP -> existing local llama.cpp
```

Tasks:

1. Create `packages/router-worker`.
2. Add minimal Worker with `/health` and `/v1/chat/completions`.
3. Add Workers VPC `MESH` binding.
4. Hardcode one Mesh IP and port in Worker vars.
5. Start existing llama.cpp server on the node.
6. Ensure local firewall permits Mesh inbound traffic.
7. Test Worker fetch to node.
8. Register Worker as AI Gateway custom provider.
9. Validate provider bearer token behavior.
10. Validate streaming and timeout behavior.

Acceptance:

- AI Gateway can complete one non-stream request.
- AI Gateway can complete one stream request.
- Worker can reach node only through Mesh path.
- Debug logs confirm auth header shape without logging values.

### Phase 2: Durable Object Registry

Goal:

```text
node heartbeat -> registry -> route to registered node
```

Tasks:

1. Add `RegistryDO`.
2. Implement node records.
3. Implement `/node/heartbeat`.
4. Implement lease expiry.
5. Implement `/admin/status`.
6. Route to registered node instead of hardcoded node.

Acceptance:

- Node appears online.
- Node disappears after missed heartbeat.
- Worker returns 429 if no ready node exists.

### Phase 3: Node Agent MVP

Goal:

```text
installable agent registers an existing local llama.cpp server
```

Tasks:

1. Create `packages/node-agent`.
2. Build Go service skeleton.
3. Detect Mesh IP.
4. Read config file.
5. Heartbeat to Worker.
6. Proxy `/v1/chat/completions` to existing local runtime.
7. Enforce upstream bearer token.
8. Serve local UI on `127.0.0.1:17777`.
9. Package Linux systemd and Windows service install commands.

Acceptance:

- Agent shows Mesh IP and last heartbeat.
- Router routes through agent rather than directly to runtime.
- Runtime can be manually started outside the agent.

### Phase 4: Engine Management

Goal:

```text
agent manages llama.cpp runtime and model files
```

Tasks:

1. Add model profile fetch from Worker.
2. Add profile version tracking.
3. Add desired-profile response on heartbeat.
4. Add model cache directory.
5. Add resumable model download.
6. Add checksum verification.
7. Add llama-server binary management.
8. Add runtime process supervisor.
9. Add Start and Stop button.
10. Add token/sec and GPU metrics.

Acceptance:

- Fresh node can download configured model.
- Start button launches runtime.
- Stop button drains and stops runtime.
- Dashboard shows useful model and performance stats.

### Phase 5: Admin Setup And One-Line Install

Goal:

```text
Worker setup UI generates install commands
```

Tasks:

1. Implement first-run setup state.
2. Implement setup-token creation.
3. Implement `/node/claim`.
4. Generate Linux/macOS install command.
5. Generate Windows PowerShell install command.
6. Serve `/install.sh`.
7. Serve `/install.ps1`.
8. Store permanent node token after claim.

Acceptance:

- Setup token is single-use.
- Install command does not contain permanent credentials.
- Node joins mesh without manual config beyond the command.

### Phase 6: Multi-Node Routing

Goal:

```text
multiple machines serve one provider endpoint
```

Tasks:

1. Add stable public model aliases.
2. Add active profile mapping per public alias.
3. Add request model rewriting.
4. Add session affinity.
5. Add reservations.
6. Add node weights.
7. Add failure penalties.
8. Add 429 busy behavior.
9. Add node revoke and drain controls.

Acceptance:

- Five nodes can register.
- One hot session stays on one node.
- Busy node is not overbooked.
- Unhealthy node is avoided.
- AI Gateway route can stay on `mesh-default` while router swaps active
  profile between Qwen and Gemma.

### Phase 7: GitHub Actions

Goal:

```text
build, release, and deploy from GitHub Actions
```

Tasks:

1. Add CI workflow.
2. Add Worker deploy workflow.
3. Add node release workflow.
4. Add checksums.
5. Publish artifacts.
6. Make install scripts resolve latest compatible release.

Acceptance:

- Push to `main` deploys Worker.
- Tag builds node-agent releases.
- Installer verifies downloaded checksum.

### Phase 8: Production Hardening

Tasks:

1. Per-node upstream tokens.
2. Token rotation.
3. Request body size limits.
4. Admin session hardening.
5. Optional Cloudflare Access for admin hostname.
6. Structured logs.
7. Artifact signing.
8. Auto-update policy.
9. R2-backed artifact mirror.
10. More backend adapters.

## Validation Gates

Do not proceed past these gates without proving them.

### Gate 1: Worker To Mesh

```text
[ ] WARP-enrolled node has Mesh IP.
[ ] Local service listens on Mesh-reachable address.
[ ] Local firewall allows inbound Mesh request.
[ ] Worker VPC binding can fetch `http://<mesh-ip>:<port>/health`.
```

### Gate 2: AI Gateway To Worker

```text
[ ] AI Gateway custom provider calls Worker.
[ ] Provider token reaches Worker as expected.
[ ] Worker rejects missing or wrong provider token.
[ ] `/v1/chat/completions` succeeds through AI Gateway.
```

### Gate 3: Streaming And Long Prefill

```text
[ ] Streamed response passes through Worker and AI Gateway.
[ ] Long prefill does not hit timeout under configured route.
[ ] AI Gateway retries are configured low enough to avoid duplicate work.
```

### Gate 4: Node Agent MVP

```text
[ ] Agent detects Mesh IP.
[ ] Agent heartbeats successfully.
[ ] Agent local UI works on localhost.
[ ] Worker routes through agent.
[ ] Agent rejects missing upstream token.
```

### Gate 5: Multi-Node Routing

```text
[ ] Multiple nodes register.
[ ] In-flight reservation prevents overbooking.
[ ] Session affinity works.
[ ] Busy behavior returns 429.
[ ] Stale nodes expire.
```

## Open Decisions

These must be nailed down before implementation reaches the relevant phase.

1. Agent artifact distribution:
   Use public GitHub Release artifacts, R2 mirror, or Worker download broker?

2. Admin auth after first setup:
   Keep `ADMIN_TOKEN` only for MVP, or add Cloudflare Access for `/admin/*`
   once the custom domain exists?

3. Runtime token model:
   Use one `NODE_UPSTREAM_TOKEN` for MVP, then migrate to per-node upstream
   tokens, or start with per-node upstream tokens immediately?

4. Model profile defaults:
   Should `mesh-default` initially target `gemma4-26b-a4b-256k-3090`,
   `qwen36-27b-256k-3090`, or a smaller fast smoke-test profile?

5. Dynamic route automation:
   Should first version create only the custom provider and print manual
   dynamic-route instructions, or should it create provider, route, route
   version, and deployment automatically?

6. BYOK automation:
   Should the Worker only show the provider token for manual BYOK entry first,
   or should it request `Secrets Store Edit` and automate key creation later?

7. Node listener binding:
   Should the agent insist on binding to Mesh IP, or allow `0.0.0.0` with
   strict token auth and firewall rules as the default fallback?

8. Hardware metrics:
   Should GPU metrics be best-effort from `nvidia-smi` first, or should the
   agent use platform-specific libraries from the start?

## Recommended First Sprint

Build the smallest vertical slice:

```text
packages/router-worker
  Worker with provider auth
  `/health`
  `/v1/chat/completions`
  hardcoded Mesh IP
  Workers VPC binding

Cloudflare setup
  AI Gateway custom provider
  one dynamic route
  provider bearer token

Local node
  existing llama.cpp server
  Mesh-reachable address
  firewall rule
```

Do not build the full node agent until this works. The risky dependency is not
Go, installers, or model downloads. The risky dependency is whether the Worker
can reliably fetch a WARP-enrolled machine through Workers VPC and Cloudflare
Mesh in the way required by AI Gateway traffic.
